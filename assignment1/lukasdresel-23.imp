decl a, w, x, y, z in

x := 0;
y := 1;
z := {0, 1};

a := x * y + w;
a := x * y + x;
a := x * y + y;
a := x * y + z;

a := y * y + w;
a := y * y + x;
a := y * y + y;
a := y * y + z;

a := z * y + w;
a := z * y + x;
a := z * y + y;
a := z * y + z;

a := y * y / w * x + y - z * x < 4 != 1 / {1} = {0, 2} <= 1 * 3 / 5;
a := x * y / w * x + y - z * x < 4 != 1 / {1} = {0, 2} <= 1 * 3 / 5;