// Primality test using trial division
decl n, factor, primality in
n := {0, 1}; // n is some random number
factor := 2;
primality := 1;
while (factor < n) {
	// Check if n % factor == 0
	if (n / factor * factor = n) {
		primality := 0;
	}
	factor := factor + 1;
}
print primality;
