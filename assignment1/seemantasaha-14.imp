decl x, y, z in
	x := {0, 2};
	y := {1, 3};
	if (x < y) {
		while ({0,1}) {
			x := x * 2;
			y := y + 2;
			z := x + y;
		}
	}
	else {
		while ({0,1}) {
			x := x / 2;
			y := y - 2;
			z := x + y;
		}
	}